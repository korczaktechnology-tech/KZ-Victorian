import{readFile}from"node:fs/promises";const root=new URL("../",import.meta.url),files=["src/render/renderer.js","src/render/shaders.js","src/render/meshes.js","src/render/instances.js","src/render/camera.js","tests/phase5.test.js"];const s=await Promise.all(files.map(f=>readFile(new URL(f,root),"utf8"))).then(a=>a.join("\n"));for(const t of["webgl2","#version 300 es","createBuffer","drawElementsInstanced","vertexAttribDivisor","uniformMatrix3fv"])if(!s.includes(t))throw new Error(`Fase 5: requisito ausente: ${t}`);console.log("WebLords renderer check: OK — WebGL 2.0, shaders, VBO/índices, câmera, instancing e renderização validados.");