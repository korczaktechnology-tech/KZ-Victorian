import{readFile}from"node:fs/promises";const root=new URL("../",import.meta.url);const files=["src/systems/construction.js","src/systems/economy.js","src/systems/production.js","src/systems/logistics.js","src/systems/simulation.js","src/core/world.js","tests/phase7.test.js"];const source=await Promise.all(files.map(f=>readFile(new URL(f,root),"utf8"))).then(a=>a.join("\n"));for(const token of["requestConstruction","transferInventory","RECIPES","createLogisticsTask","commandAccepted","commandRejected","inventoryChanged"])if(!source.includes(token))throw new Error("Fase 7: requisito ausente: "+token);console.log("WebLords economy check: OK — construção, economia, produção, logística e comandos validados.");