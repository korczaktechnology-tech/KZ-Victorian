import test from"node:test";import assert from"node:assert/strict";import{DEFAULT_VERTEX_SHADER,DEFAULT_FRAGMENT_SHADER}from"../src/render/shaders.js";import{UNIT_QUAD,UNIT_QUAD_INDICES}from"../src/render/meshes.js";import{Camera}from"../src/render/camera.js";import{InstanceBuffer}from"../src/render/instances.js";import{cullInstances}from"../src/render/culling.js";import{createGPUProfiler}from"../src/render/gpu-profiler.js";
test("Fase 5: shaders GLSL ES 3.00",()=>{assert.match(DEFAULT_VERTEX_SHADER,/#version 300 es/);assert.match(DEFAULT_VERTEX_SHADER,/a_instancePosition/);assert.match(DEFAULT_VERTEX_SHADER,/a_instanceScale/);assert.match(DEFAULT_FRAGMENT_SHADER,/#version 300 es/)});
test("Fase 5: mesh indexado",()=>{assert.equal(UNIT_QUAD.length,8);assert.equal(UNIT_QUAD_INDICES.length,6)});
test("Fase 5: câmera e view",()=>{const c=new Camera().setPosition(2,3).setZoom(2),m=c.getMatrix(2),v=c.getView(2);assert.equal(m[0],1);assert.equal(m[4],2);assert.equal(m[6],-2);assert.equal(m[7],-6);assert.equal(v.x,2);assert.equal(v.aspect,2)});
test("Fase 5: instâncias limitadas",()=>{const b=new InstanceBuffer({createBuffer:()=>({})},10);b.resize(4);assert.equal(b.count,4);assert.equal(b.data.length,30)});
test("Fase 5: culling remove objetos fora da câmera",()=>{const r=cullInstances(new Float32Array([0,0,0,100,100,0,-100,-100,0]),10,{x:0,y:0,zoom:1,aspect:1});assert.equal(r.count,1);assert.equal(r.culled,2)});
test("Fase 5: GPU profiler expõe suporte sem mascarar ausência",()=>{const p=createGPUProfiler({getExtension:()=>null});assert.equal(p.supported,false);assert.equal(p.resolve(),0)});
test("Fase 5: WebGL2 é obrigatório",async()=>{const{createRenderer}=await import("../src/render/renderer.js");assert.throws(()=>createRenderer({getContext:()=>null}),/WebGL 2.0 não está disponível/)});
