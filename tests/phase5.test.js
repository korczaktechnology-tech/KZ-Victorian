import test from"node:test";import assert from"node:assert/strict";import{DEFAULT_VERTEX_SHADER,DEFAULT_FRAGMENT_SHADER}from"../src/render/shaders.js";import{UNIT_QUAD,UNIT_QUAD_INDICES}from"../src/render/meshes.js";import{Camera}from"../src/render/camera.js";import{InstanceBuffer}from"../src/render/instances.js";test("Fase 5: shaders",()=>{assert.match(DEFAULT_VERTEX_SHADER,/#version 300 es/);assert.match(DEFAULT_VERTEX_SHADER,/a_instancePosition/);assert.match(DEFAULT_VERTEX_SHADER,/a_instanceScale/);assert.match(DEFAULT_FRAGMENT_SHADER,/#version 300 es/)});test("Fase 5: mesh",()=>{assert.equal(UNIT_QUAD.length,8);assert.equal(UNIT_QUAD_INDICES.length,6)});test("Fase 5: camera",()=>{const c=new Camera().setPosition(2,3).setZoom(2),m=c.getMatrix(2);assert.equal(m[0],1);assert.equal(m[4],2);assert.equal(m[6],-2);assert.equal(m[7],-6)});test("Fase 5: instances",()=>{const b=new InstanceBuffer({createBuffer:()=>({})},10);b.resize(4);assert.equal(b.data.length,12)});test("Fase 5: WebGL2 required",async()=>{const{createRenderer}=await import("../src/render/renderer.js");assert.throws(()=>createRenderer({getContext:()=>null}),/WebGL 2.0 não está disponível/)});