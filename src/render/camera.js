export class Camera{constructor(){this.x=0;this.y=0;this.zoom=1;}setPosition(x,y){this.x=Number(x)||0;this.y=Number(y)||0;return this;}setZoom(z){this.zoom=Math.max(.05,Number(z)||1);return this;}getMatrix(aspect=1){const sx=this.zoom/Math.max(aspect,.0001),sy=this.zoom;return new Float32Array([sx,0,0,0,sy,0,-this.x*sx,-this.y*sy,1]);}getView(aspect=1){return Object.freeze({x:this.x,y:this.y,zoom:this.zoom,aspect:Math.max(.0001,aspect)});}}