import{createProgram}from"./shaders.js";import{createQuadMesh}from"./meshes.js";import{InstanceBuffer}from"./instances.js";import{Camera}from"./camera.js";export function createRenderer(canvas,o={}){const gl=canvas.getContext("webgl2",{antialias:false,alpha:false});if(!gl)throw new Error("WebLords: WebGL 2.0 não está disponível neste navegador.");const program=createProgram(gl),mesh=createQuadMesh(gl),instances=new InstanceBuffer(gl,o.maxInstances??4096),camera=o.camera??new Camera(),vao=gl.createVertexArray();if(!vao)throw new Error("WebLords: falha ao criar VAO.");const p=gl.getAttribLocation(program,"a_position"),ip=gl.getAttribLocation(program,"a_instancePosition"),s=gl.getAttribLocation(program,"a_instanceScale"),cam=gl.getUniformLocation(program,"u_camera"),color=gl.getUniformLocation(program,"u_color");if(p<0||ip<0||s<0||!cam||!color)throw new Error("WebLords: atributos/uniformes obrigatórios não encontrados.");gl.bindVertexArray(vao);gl.bindBuffer(gl.ARRAY_BUFFER,mesh.vertexBuffer);gl.enableVertexAttribArray(p);gl.vertexAttribPointer(p,2,gl.FLOAT,false,0,0);gl.bindBuffer(gl.ARRAY_BUFFER,instances.buffer);gl.enableVertexAttribArray(ip);gl.vertexAttribPointer(ip,2,gl.FLOAT,false,12,0);gl.vertexAttribDivisor(ip,1);gl.enableVertexAttribArray(s);gl.vertexAttribPointer(s,1,gl.FLOAT,false,12,8);gl.vertexAttribDivisor(s,1);gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,mesh.indexBuffer);gl.bindVertexArray(null);let drawCalls=0;function resize(){const d=Math.max(1,window.devicePixelRatio||1),w=Math.max(1,Math.floor(canvas.clientWidth*d)),h=Math.max(1,Math.floor(canvas.clientHeight*d));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h;}gl.viewport(0,0,w,h);}function render(time,snapshot={}){resize();const pos=snapshot.positions??[],count=Math.min(o.maxInstances??4096,Math.floor(pos.length/3));instances.resize(count);instances.data.set(pos.slice(0,count*3));instances.upload(count);gl.clearColor(.03,.03,.03,1);gl.clear(gl.COLOR_BUFFER_BIT);gl.useProgram(program);gl.uniformMatrix3fv(cam,false,camera.getMatrix(canvas.width/canvas.height));gl.uniform4f(color,.75,.82,.92,1);gl.bindVertexArray(vao);if(count){gl.drawElementsInstanced(gl.TRIANGLES,mesh.indexCount,gl.UNSIGNED_SHORT,0,count);drawCalls=1;}else drawCalls=0;gl.bindVertexArray(null);}return{render,getContext:()=>gl,getCamera:()=>camera,getDrawCalls:()=>drawCalls,isWebGL2:true};}