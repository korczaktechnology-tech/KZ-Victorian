import{createProgram}from"./shaders.js";import{createQuadMesh}from"./meshes.js";import{InstanceBuffer}from"./instances.js";import{Camera}from"./camera.js";import{cullInstances}from"./culling.js";import{createGPUProfiler}from"./gpu-profiler.js";import{createAssetProfiler}from"../core/asset-profiler.js";import{createTerrainMesh}from"./terrain.js";import{TERRAIN_VERTEX_SHADER,TERRAIN_FRAGMENT_SHADER}from"./terrain-shaders.js";
export function createRenderer(canvas,o={}){const maxInstances=o.maxInstances??4096,profiler=o.assetProfiler??createAssetProfiler(),gl=canvas.getContext("webgl2",{antialias:true,alpha:false});if(!gl)throw new Error("WebLords: WebGL 2.0 não está disponível neste navegador.");profiler.attachWebGL2(gl);const gpu=createGPUProfiler(gl),camera=o.camera??new Camera(),terrain=createTerrainMesh(gl,256,256),terrainProgram=createProgram(gl,TERRAIN_VERTEX_SHADER,TERRAIN_FRAGMENT_SHADER),terrainVao=gl.createVertexArray();if(!terrainVao)throw new Error("WebLords: falha ao criar VAO do terreno.");const tp=gl.getAttribLocation(terrainProgram,"a_position"),tn=gl.getAttribLocation(terrainProgram,"a_normal"),tmat=gl.getUniformLocation(terrainProgram,"u_viewProjection");if(tp<0||tn<0||!tmat)throw new Error("WebLords: atributos/uniforme do terreno não encontrados.");gl.bindVertexArray(terrainVao);gl.bindBuffer(gl.ARRAY_BUFFER,terrain.positionBuffer);gl.enableVertexAttribArray(tp);gl.vertexAttribPointer(tp,3,gl.FLOAT,false,0,0);gl.bindBuffer(gl.ARRAY_BUFFER,terrain.normalBuffer);gl.enableVertexAttribArray(tn);gl.vertexAttribPointer(tn,3,gl.FLOAT,false,0,0);gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,terrain.indexBuffer);gl.bindVertexArray(null);let drawCalls=0,visibleInstances=0,culledInstances=0,lastMetrics={drawCalls:0,visibleInstances:0,culledInstances:0,gpuMs:0,gpuTimerSupported:gpu.supported};function resize(){const d=Math.max(1,window.devicePixelRatio||1),w=Math.max(1,Math.floor(canvas.clientWidth*d)),h=Math.max(1,Math.floor(canvas.clientHeight*d));if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h;}gl.viewport(0,0,w,h);}function render(time,snapshot={}){const frame=profiler.beginFrame(),gpuQuery=gpu.begin();resize();gl.enable(gl.DEPTH_TEST);gl.depthFunc(gl.LEQUAL);gl.clearColor(.035,.055,.025,1);gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);gl.useProgram(terrainProgram);gl.uniformMatrix4fv(tmat,false,camera.getViewProjection(canvas.width/canvas.height));gl.bindVertexArray(terrainVao);gl.drawElements(gl.TRIANGLES,terrain.indexCount,gl.UNSIGNED_INT,0);gl.bindVertexArray(null);const pos=snapshot.positions??[];const view=camera.getView(canvas.width/canvas.height),culled=cullInstances(pos,maxInstances,view);visibleInstances=culled.count;culledInstances=culled.culled;drawCalls=1;gpu.end(gpuQuery);const gpuMs=gpu.resolve();profiler.endFrame(frame);lastMetrics={drawCalls,visibleInstances,culledInstances,gpuMs,gpuTimerSupported:gpu.supported};}return{render,getContext:()=>gl,getCamera:()=>camera,getDrawCalls:()=>drawCalls,getMetrics:()=>({...lastMetrics}),isWebGL2:true,getProfiler:()=>profiler,getGPUProfiler:()=>gpu};}