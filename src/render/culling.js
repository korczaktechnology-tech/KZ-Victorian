export function cullInstances(positions,maxInstances,view){const count=Math.min(maxInstances,Math.floor(positions.length/3));const visible=new Float32Array(count*3);let n=0;const halfX=Math.max(.0001,1/view.zoom)*view.aspect,halfY=Math.max(.0001,1/view.zoom);for(let i=0;i<count;i++){const x=positions[i*3],y=positions[i*3+1],z=positions[i*3+2];if(x<view.x-halfX||x>view.x+halfX||y<view.y-halfY||y>view.y+halfY)continue;visible[n*3]=x;visible[n*3+1]=y;visible[n*3+2]=z;n++;}return{data:visible.subarray(0,n*3),count:n,culled:count-n};}