const DEFAULT_SIZE=256,DEFAULT_SEGMENTS=256;
function terrainHeight(x,y,size){const nx=x/size,ny=y/size;const broad=Math.sin(nx*Math.PI*2.1+.35)*2.6+Math.cos(ny*Math.PI*1.7-.8)*2.1;const medium=Math.sin((nx+ny)*Math.PI*5.2)*.85+Math.cos((nx-ny)*Math.PI*4.1)*.7;const fine=Math.sin(nx*Math.PI*11+Math.sin(ny*8))*.22+Math.cos(ny*Math.PI*13+Math.cos(nx*7))*.18;const valley=-1.7*Math.exp(-(((nx-.56)**2)/.035+((ny-.43)**2)/.055));const hill=1.5*Math.exp(-(((nx-.24)**2)/.025+((ny-.72)**2)/.035));return Math.max(-1.5,broad+medium+fine+valley+hill);}
export function createTerrainMesh(gl,size=DEFAULT_SIZE,segments=DEFAULT_SEGMENTS){if(!gl)throw new TypeError("WebLords: contexto WebGL inválido para o terreno.");const cells=Math.max(1,segments|0),side=cells+1,count=side*side,positions=new Float32Array(count*3),normals=new Float32Array(count*3),step=size/cells,height=(x,y)=>terrainHeight(Math.max(0,Math.min(size,x)),Math.max(0,Math.min(size,y))),sample=(x,y)=>height(x,y);let i=0;for(let z=0;z<=cells;z++){const y=z*step;for(let x=0;x<=cells;x++){const px=x*step,h=height(px,y),p=i*3,dx=(sample(px+step,y)-sample(px-step,y))/(2*step),dz=(sample(px,y+step)-sample(px,y-step))/(2*step),nx=-dx,ny=1,nz=-dz,l=Math.hypot(nx,ny,nz)||1;positions[p]=px-size/2;positions[p+1]=h;positions[p+2]=y-size/2;normals[p]=nx/l;normals[p+1]=ny/l;normals[p+2]=nz/l;i++;}}const indices=new Uint32Array(cells*cells*6);let k=0;for(let z=0;z<cells;z++)for(let x=0;x<cells;x++){const a=z*side+x,b=a+1,c=a+side,d=c+1;indices[k++]=a;indices[k++]=c;indices[k++]=b;indices[k++]=b;indices[k++]=c;indices[k++]=d;}const positionBuffer=gl.createBuffer(),normalBuffer=gl.createBuffer(),indexBuffer=gl.createBuffer();if(!positionBuffer||!normalBuffer||!indexBuffer)throw new Error("WebLords: falha ao criar buffers do terreno.");gl.bindBuffer(gl.ARRAY_BUFFER,positionBuffer);gl.bufferData(gl.ARRAY_BUFFER,positions,gl.STATIC_DRAW);gl.bindBuffer(gl.ARRAY_BUFFER,normalBuffer);gl.bufferData(gl.ARRAY_BUFFER,normals,gl.STATIC_DRAW);gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,indexBuffer);gl.bufferData(gl.ELEMENT_ARRAY_BUFFER,indices,gl.STATIC_DRAW);return Object.freeze({size,cells,vertexCount:count,indexCount:indices.length,positionBuffer,normalBuffer,indexBuffer});}
