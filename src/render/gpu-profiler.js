export function createGPUProfiler(gl){const ext=gl?.getExtension?.("EXT_disjoint_timer_query_webgl2");const pending=[];let lastGpuMs=0;function begin(){if(!ext)return null;const q=gl.createQuery();gl.beginQuery(ext.TIME_ELAPSED_EXT,q);return q;}function end(q){if(!q)return;gl.endQuery(ext.TIME_ELAPSED_EXT);pending.push(q);}function resolve(){if(!ext)return lastGpuMs;const keep=[];for(const q of pending){if(gl.getQueryParameter(q,gl.QUERY_RESULT_AVAILABLE)){if(!gl.getParameter(ext.GPU_DISJOINT_EXT))lastGpuMs=Number(gl.getQueryParameter(q,gl.QUERY_RESULT))/1e6;gl.deleteQuery(q);}else keep.push(q);}pending.length=0;pending.push(...keep);return lastGpuMs;}return{supported:!!ext,begin,end,resolve,getLastGpuMs:()=>lastGpuMs};}